# Sample configuration file for cdrd

# Panasonic KX-TDA200
#
#line_speed="19200"
#
# Reject SMDR headers
#exclude_re="^(-+|[[:space:]]+Date +Time +Ext CO +Dial Number +Ring Duration +Acc code +CD)[[:space:]]*$"

# Panasonic KX-TES824, KX-TEM824
#
# Reject SMDR headers
#exclude_re="^(-+| +Date +Time +Ext\. +CO +Dial number +Duration +Code)?[[:space:]]*$"
